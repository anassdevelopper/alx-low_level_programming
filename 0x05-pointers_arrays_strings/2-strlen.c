#include "main.h"

/**
 * _strlen -returns the length of a string
 * @s: string
 * Return: returns an integer
 */

int _strlen(char *s)
{
	int i;

	for (i = 0; s[index] != '\0'; i++)
		;
	return (i);
}
